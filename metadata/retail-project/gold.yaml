config:
  file_format: csv
  output_options:
    header: true
    repartition: 1
  source_path: s3://retail-project-silver/
  target_path: s3://retail-project-gold/
layer: gold
project_id: retail-project
