config:
  file_format: csv
  output_options:
    header: true
    repartition: 1
  source_path: s3://retail-silver-layer/
  target_path: s3://retail-gold-layer/
layer: gold
project_id: retail-project
