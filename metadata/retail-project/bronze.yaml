config:
  file_format: csv
  output_options:
    header: true
    repartition: 1
  source_path: s3://retail-raw-data/
  target_path: s3://retail-bronze-layer/
layer: bronze
project_id: retail-project
