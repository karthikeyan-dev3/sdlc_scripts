config:
  file_format: csv
  output_options:
    header: true
    repartition: 1
  source_path: s3://retail-project-bronze/
  target_path: s3://retail-project-silver/
layer: silver
project_id: retail-project
